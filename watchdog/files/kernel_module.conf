{%- from "watchdog/map.jinja" import server with context %}
{%- if server.kernel_module %}
option {{ server.kernel_module }}{% for (parameter, value) in kernel.parameters.items() %} {{ parameter }}{%if value %}={{ value }}{% endif %}{% endfor %}
{%- endif %}
