{%- from "watchdog/map.jinja" import server with context %}
{%- if server.kernel_module is defined %}
option {{ server.kernel_module }}{% for (parameter, value) in server.kernel.parameters.items() %} {{ parameter }}{%if value %}={{ value }}{% endif %}{% endfor %}
{%- endif %}
